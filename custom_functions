pman() { man -t "$@" | open -f -a /Applications/Preview.app  }

card() {
  if [ -e "${HOME}/.cards/${1}" ]
  then
    cat ${HOME}/.cards/${1}
  else
    cat $(grep -ilrE "$1" ${HOME}/.cards | sed 's/^(.*)$/"&"/g') | grep --color -Ei "${1}|$"
  fi
}


